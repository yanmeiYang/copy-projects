import React, { Component } from 'react';
import styles from './round.less';

export default class Round extends Component {
  render() {
    return (
      <i className={styles.round} />
    )
  }
}
